<!-- markdownlint-disable MD041 -->
<!-- A pull request body is a fragment rather than a document, so it opens at H2;
     an H1 renders oversized in one. The title becomes the squash subject with ` (#N)` appended. Write it as a
     Conventional Commit: imperative, lowercase, and 50 characters or fewer as
     typed -- the number GitHub appends is the platform's and is not counted.
     `gitlint` holds every local commit to the same bound, and the PR mechanical
     checks hold the title to it. -->

## What changes, and why

## Corpus

The corpus is the specification, and both implementations answer to it. Tick what applies.

- [ ] This changes no behavior the corpus specifies.
- [ ] This changes what gets joined, and a case in `corpus/` pins the new behavior. The case was written first and failed first.
- [ ] The change makes the tool join **more** than it did. The section above says what it leaves alone.

## Checks

- [ ] `make check` passes, or `make test` does and this touches no Rust.

<!-- Answer keys are generated by running the tool, never written by hand and never
     edited to make a test pass. corpus/README.md and corpus/cli/README.md say how. -->
