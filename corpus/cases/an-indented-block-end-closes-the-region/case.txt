name: An indented closing marker closes the region
why: Both markers are matched after leading whitespace is stripped, so a marker written at a list item's indentation still closes. This follows from the line form, whose matcher strips the same way, and it is what lets a region end inside the item it started in.
paragraphs_unwrapped: 1
line_breaks_removed: 1
