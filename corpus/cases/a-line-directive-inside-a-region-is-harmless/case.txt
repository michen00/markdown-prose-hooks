name: A line-level directive inside a region neither errors nor does anything
why: The two forms are one feature and a document may carry both. Inside a region the line form has nothing left to exempt, so it passes through as the comment it is. Pinned because an implementation that armed the line flag inside a region could carry it past the closing marker and exempt a paragraph nobody marked.
paragraphs_unwrapped: 1
line_breaks_removed: 1
