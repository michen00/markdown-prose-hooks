name: A multi-line directive inside YAML front matter is not a directive
why: Front matter is data, and the guard that passes it through runs before any marker is looked for. The wider rule reads a comment across several lines, which is exactly the shape front matter is full of, so the ordering that used to be free is worth pinning now that more lines reach it.
paragraphs_unwrapped: 1
line_breaks_removed: 1
