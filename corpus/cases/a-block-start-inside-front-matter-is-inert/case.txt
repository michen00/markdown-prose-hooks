name: An opening marker inside YAML front matter is not a marker
why: Front matter is data the tool passes through, so a marker-shaped line in it is a string value rather than an instruction. Inertness comes from guard order here too, the same way the fence case gets it.
paragraphs_unwrapped: 1
line_breaks_removed: 1
