name: A directive inside YAML front matter is not a directive
why: Front matter is data, and the guard that passes it through runs before the directive is looked for. Pinned because the two are only ordered correctly by accident otherwise.
paragraphs_unwrapped: 1
line_breaks_removed: 1
