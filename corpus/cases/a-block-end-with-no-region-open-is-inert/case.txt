name: A closing marker with no region open changes nothing
why: A marker left behind by an edit that removed its opener should not be an error, and there is nothing for it to do. Every tool measured treats the orphan the same way, and an exit code here would fail a document that renders correctly.
paragraphs_unwrapped: 1
line_breaks_removed: 1
