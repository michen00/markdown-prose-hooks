name: A multi-line comment holding more than the marker is left as prose
why: The match is on the whole trimmed inner content, exactly, which is the same discipline the one-line form applies and the reason reading inside a comment is safe at all. Without it every note a human wrote about the tool, spread over the lines a note is naturally spread over, would become an instruction to it. This is the guard the wider rule has to keep, so it is pinned rather than inferred from the one-line case.
paragraphs_unwrapped: 1
line_breaks_removed: 1
