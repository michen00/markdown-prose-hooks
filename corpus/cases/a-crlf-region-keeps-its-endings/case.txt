name: A region in a CRLF file keeps every ending it arrived with
why: Lines inside a region are emitted as the bytes they arrived as, which is the whole of the guarantee. A region built by rejoining stripped content would rewrite a CRLF file to LF and pass a test written in LF, so this case is bytes on disk rather than an assertion about them.
paragraphs_unwrapped: 1
line_breaks_removed: 1
