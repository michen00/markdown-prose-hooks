name: Every container inside a region passes through unchanged
why: A region suspends the transform rather than narrowing it, so a wrapped list item and a wrapped blockquote inside one are left as written. Without the region all three paragraphs here would join, which is what makes this case pin the suspension rather than restate the input.
paragraphs_unwrapped: 1
line_breaks_removed: 1
